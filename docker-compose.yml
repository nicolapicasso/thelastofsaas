services:
  # PHP + Apache
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tlos-web
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./docker/apache/vhost.conf:/etc/apache2/sites-available/000-default.conf
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=tlos
      - DB_USER=tlos
      - DB_PASS=tlos_secret
      - APP_ENV=development
      - APP_DEBUG=true
    networks:
      - tlos-network

  # MySQL 8.0
  db:
    image: mysql:8.0
    container_name: tlos-db
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root_secret
      MYSQL_DATABASE: tlos
      MYSQL_USER: tlos
      MYSQL_PASSWORD: tlos_secret
    volumes:
      - db_data:/var/lib/mysql
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./database/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql
      - ./migrations/021_tlos_core_schema.sql:/docker-entrypoint-initdb.d/03-tlos-schema.sql
    command: --default-authentication-plugin=mysql_native_password
    networks:
      - tlos-network

  # phpMyAdmin (opcional, para gestionar la BD)
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: tlos-phpmyadmin
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_USER: tlos
      PMA_PASSWORD: tlos_secret
    depends_on:
      - db
    networks:
      - tlos-network

volumes:
  db_data:

networks:
  tlos-network:
    driver: bridge
